# .env.example

# Your OpenAI API Key (keep this secret in .env, never commit the real one)
OPENAI_API_KEY="tu clave de API aquí"

# Choose which model to use (e.g. gpt-5-nano, gpt-5-mini, gpt-4o-mini)
OPENAI_MODEL="gpt-3.5-turbo"

# Whether to run in mock mode (true = free local test, false = real API call)
USE_MOCK=true